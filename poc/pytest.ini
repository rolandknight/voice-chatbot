[pytest]
markers =
    smoke: fast end-to-end smoke tests (T1, T2)
    tools: tool-selection tests (T3, T4)
    duplex: full-duplex barge-in tests (T5); requires the duplex server build
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
