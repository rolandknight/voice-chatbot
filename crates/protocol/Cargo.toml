[package]
name = "voice-chatbot-protocol"
version = "0.1.0"
edition = "2021"
publish = false
description = "Server<->client message types shared by crates/server and crates/client"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
