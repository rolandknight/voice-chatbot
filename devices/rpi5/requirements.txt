aiortc>=1.9
# HTTP client for the WebRTC signaling POST (offer -> answer). Directly
# imported by rpi_webrtc_voice.py; aiortc does not pull it in transitively.
aiohttp>=3.9
fastapi>=0.110
uvicorn[standard]>=0.27

# On-device wake audio I/O (Pi + dev Mac). sounddevice also backs macOS
# WebRTC playback (--audio-format=avfoundation).
sounddevice>=0.4
numpy>=1.24

# openWakeWord runtime deps (the ONNX inference path). openWakeWord itself is
# installed separately with --no-deps (see install_rpi.sh), because its
# packaging pins a Linux-only `tflite-runtime` that has no wheels for
# Python >= 3.12 — and we don't use tflite. Installing these here means the
# `pip install --no-deps openwakeword` step has everything it needs and works
# on both Python 3.11 and 3.12.
onnxruntime>=1.10
tqdm
scipy
scikit-learn
requests

# Spotify control helper (scripts/spotify.py --list-devices / --bootstrap).
# The Pi doesn't play Spotify through Python — librespot does — but running the
# device-visibility check and the one-time OAuth on the Pi needs the control
# client (spotipy + loguru) and the shared `config` package (pydantic + dotenv).
loguru
spotipy
pydantic>=2
pydantic-settings>=2
python-dotenv
